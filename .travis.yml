sudo: required
dist: precise
language: c

services:
    - docker

matrix:
  include:
    - env: PLATFORM=ubuntu/precise
    - env: PLATFORM=ubuntu/trusty

script:
  - docker --version
  - docker-compose --version
  - make -C ci $PLATFORM

# vim:et:ts=2:sw=2
